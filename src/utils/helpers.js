// export const updateItems = arr => {
//   const itemsLS = JSON.parse(localStorage.getItem('bag'))
//   const foundItems = arr.filter(item => itemsLS.some(itemLS => itemLS.id === item.id))
//
//   return foundItems
// }
